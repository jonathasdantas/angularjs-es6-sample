import { should } from 'chai';

//import AirportSelectorModule from './airport-selector.module';

describe('Airport Selector Component Tests', () => {
  let component;

  beforeEach(() => {
    // angular.mock.module(AirportSelectorModule);

    // inject(($injector) => {
    //   component = $injector.get('AirportSelectorComponent');
    // });
  });

  it('Check if the Airport was Selected', () => {
    
  });
});
