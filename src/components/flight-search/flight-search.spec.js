import { should } from 'chai';

//import FlightSearchModule from './flight-search.module';

describe('Flight Search Component Tests', () => {
  let component;

  beforeEach(() => {
    // angular.mock.module(FlightSearchModule);

    // inject(($injector) => {
    //   component = $injector.get('FlightSearchComponent');
    // });
  });

  it('Check only sent with all params filled', () => {
    
  });
});
