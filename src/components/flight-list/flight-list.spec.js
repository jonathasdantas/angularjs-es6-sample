import { should } from 'chai';

//import FlightListModule from './flight-list.module';

describe('Flight List Component Tests', () => {
  let component;

  beforeEach(() => {
    // angular.mock.module(FlightListModule);

    // inject(($injector) => {
    //   component = $injector.get('FlightListComponent');
    // });
  });

  it('Check if fields are filled from URL', () => {
    
  });
});
