import { should } from 'chai';

//import AirportSelectorWrapperModule from './airport-selector-wrapper.module';

describe('Airport Selector Wrapper Component Tests', () => {
  let component;

  beforeEach(() => {
    // angular.mock.module(AirportSelectorWrapperModule);

    // inject(($injector) => {
    //   component = $injector.get('AirportWrapperSelectorComponent');
    // });
  });

  it('Check both Airports Selected', () => {
    
  });
});